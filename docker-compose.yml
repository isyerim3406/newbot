version: '3.8'
services:
  app:
    build: .
    container_name: tradingview-bot
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"  # Eğer web server varsa
    volumes:
      - ./:/app
